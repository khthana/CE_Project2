 create table personalbook
 ( no_per  integer primary key,
 name  varchar(20),
 lastname varchar(20),
 pager  integer,
 pagernumber integer,
 pagerpwd varchar(10),
 email varchar(30),
 mobile integer,
 mobilepwd varchar(20),
 idgroup varchar(20),
 address varchar(30),
 telephone integer,
 status varchar(20),
 note   varchar(50));
 alter table personalbook
add no_appoint integer references appoint;
 alter table personalbook
 add no_rem integer references remind;
