create table repeat(
no_rep integer primary key,
begin date,
until date,
freq_rep varchar(30),
id varchar(20) NOT NULL REFERENCES userdata);

 create table repeat(
 no_rep integer primary key,
 until date,
 freq_rep varchar(50),
 id varchar(20) NOT NULL REFERENCES userdata on delete cascade);
 
  create sequence no_rep
 START WITH  1
  INCREMENT BY 1
  NOMAXVALUE
  nominvalue
  NOCYCLE
  nocache
  order;
  
   insert into repeat(id,no_rep)
 values(' Siam2',no_rep.nextval);
 
 create sequence no
 START WITH 1
  INCREMENT BY 1
  NOMAXVALUE
  nominvalue
  NOCYCLE
  nocache
  order;
  
  select no from test
where no = no.currval;