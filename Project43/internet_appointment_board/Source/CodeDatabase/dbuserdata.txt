userdata1
SQL> create table userdata(
     id        varchar(20) primary key,
     pwd       varchar(15),
     firstname  varchar(20),
     lastname    varchar(20),
     sex          varchar(6),
     bmonth        varchar(15),
     bday           integer,
     byear          integer,
    country        varchar(20),
    zip            integer,
    occupation      varchar(30),
    email            varchar(30),
    pager            integer,
    pagernumber integer,
    pagerpwd          varchar(20),
    mobile            integer,
    mobilepwd          varchar(20),
    icq                integer);
    
    
     create table userdata(
      id        varchar(20) primary key,
      pwd       varchar(15),
      firstname  varchar(20),
      lastname    varchar(20),
      sex          varchar(6),
      bmonth        varchar(15),
      bday           integer,
      byear          integer,
      email          varchar(30),
      occupation      varchar(30),
       country        varchar(20),
      zipcode         number(10),
      pager            integer,
      pagernumber integer,
      pagerpwd          varchar(20),
      mobile            integer,
     mobilepwd          varchar(20),
      icqnumber         number(10));
