/***********************************************************************\
|         File: Person.EmpType.CDL                                      |
|       Author: Cache Objects Version 4.0.818.0                         |
|         Date: Mar 27 2001 18:35:57                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class Person.EmpType
{

	super = %Library.Persistent;

	persistent;

	relationship EmpPoss { type = Person.EmpPos; cardinality = many; inverse = EmpType; transient = 0; }
	attribute EmpType { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }

	method EmpTypeDISPLAYLIST()
	{
		returntype = %Library.String;
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new i,st
			set st=""
			set j=^Person.EmpTypeD
			for i=1:1:j {
			set Per=##class(Person.EmpType).%OpenId(i)
			set st=st_","_Per.EmpType
			do Per.%Close()
			}
			quit st
		]
	}

	storage Default
	{
		type = %Library.CacheStorage;
		datalocation = ^Person.EmpTypeD;
		indexlocation = ^Person.EmpTypeI;
		idlocation = ^Person.EmpTypeD;
		defaultdata = EmpTypeDefaultData;
		data EmpTypeDefaultData
		{
			structure = listnode;
			value (1)  = EmpType;
		}
	}

}
