/***********************************************************************\
|         File: Person.Seminar.CDL                                      |
|       Author: Cache Objects Version 4.0.818.0                         |
|         Date: Mar 27 2001 18:37:44                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class Person.Seminar
{

	super = %Library.Persistent;

	persistent;

	attribute BeginDate { type = %Library.Date; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute FinishDate { type = %Library.Date; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Name { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Place { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	relationship RSeminars { type = Person.RSeminar; cardinality = many; inverse = Seminar; transient = 0; }

	method NewSeminar(Name:%Library.String,Place:%Library.String,BeginDate:%Library.Date,FinishDate:%Library.Date)
	{
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new a
			set a=##class(Person.Seminar).%New()
			set a.Name=Name
			set a.Place=Place
			set a.BeginDate=BeginDate
			set a.FinishDate=FinishDate
			do a.%Save()
			do a.%Close()
		]
	}

	method SeminarDISPLAYLIST()
	{
		returntype = %Library.String;
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new i,st
			set st=""
			set j=^Person.SeminarD
			for i=1:1:j {
				set Per=##class(Person.Seminar).%OpenId(i)
				set st=st_","_Per.Name
				do Per.%Close()
			}
			quit st
		]
	}

	storage Default
	{
		type = %Library.CacheStorage;
		datalocation = ^Person.SeminarD;
		indexlocation = ^Person.SeminarI;
		idlocation = ^Person.SeminarD;
		defaultdata = SeminarDefaultData;
		data SeminarDefaultData
		{
			structure = listnode;
			value (1)  = BeginDate;
			value (2)  = FinishDate;
			value (3)  = Name;
			value (4)  = Place;
		}
	}

}
