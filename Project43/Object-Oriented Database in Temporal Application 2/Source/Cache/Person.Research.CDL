/***********************************************************************\
|         File: Person.Research.CDL                                     |
|       Author: Cache Objects Version 4.0.818.0                         |
|         Date: Mar 27 2001 18:37:09                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class Person.Research
{

	super = %Library.Persistent;

	persistent;

	attribute BeginDate { type = %Library.Date; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute FinishDate { type = %Library.Date; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Name { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Place { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	relationship RResearchs { type = Person.RResearch; cardinality = many; inverse = Research; transient = 0; }

	method NewResearch(Name:%Library.String,Place:%Library.String,BeginDate:%Library.Date,FinishDate:%Library.Date)
	{
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new a
			set a=##class(Person.Research).%New()
			set a.Name=Name
			set a.Place=Place
			set a.BeginDate=BeginDate
			set a.FinishDate=FinishDate
			do a.%Save()
			do a.%Close()
		]
	}

	method ResearchDISPLAYLIST()
	{
		returntype = %Library.String;
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new i,st
			set st=""
			set j=^Person.ResearchD
			for i=1:1:j {
				set Per=##class(Person.Research).%OpenId(i)
				set st=st_","_Per.Name
				do Per.%Close()
			}
			quit st
		]
	}

	storage Default
	{
		type = %Library.CacheStorage;
		datalocation = ^Person.ResearchD;
		indexlocation = ^Person.ResearchI;
		idlocation = ^Person.ResearchD;
		defaultdata = ResearchDefaultData;
		data ResearchDefaultData
		{
			structure = listnode;
			value (1)  = BeginDate;
			value (2)  = FinishDate;
			value (3)  = Name;
			value (4)  = Place;
		}
	}

}
