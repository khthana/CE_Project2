/***********************************************************************\
|         File: Person.Education.CDL                                    |
|       Author: Cache Objects Version 4.0.818.0                         |
|         Date: Mar 27 2001 18:35:39                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class Person.Education
{

	super = %Library.Persistent;

	persistent;

	attribute EduIns { type = Person.EduIns; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute EduMajor { type = Person.EduMajor; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Education { type = %Library.String(DISPLAYLIST="",VALUELIST=""); calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	relationship REducations { type = Person.REducation; calculated = 0; not final; cardinality = many; inverse = Education; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }

	method EducationDISPLAYLIST()
	{
		returntype = %Library.String;
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new i,st
			set st=""
			set j=^Person.EducationD
			for i=1:1:j {
				set Per=##class(Person.Education).%OpenId(i)
				set st=st_","_Per.Education
				do Per.%Close()
			}
			quit st
		]
	}

	storage Default
	{
		type = %Library.CacheStorage;
		datalocation = ^Person.EducationD;
		indexlocation = ^Person.EducationI;
		idlocation = ^Person.EducationD;
		defaultdata = EducationDefaultData;
		data EducationDefaultData
		{
			structure = listnode;
			value (1)  = Education;
			value (2)  = EduIns;
			value (3)  = EduMajor;
		}
	}

}
