/***********************************************************************\
|         File: Person.Training.CDL                                     |
|       Author: Cache Objects Version 4.0.818.0                         |
|         Date: Mar 27 2001 18:38:07                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class Person.Training
{

	super = %Library.Persistent;

	persistent;

	attribute BeginDate { type = %Library.Date; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute FinishDate { type = %Library.Date; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Name { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Place { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	relationship RTrainings { type = Person.RTraining; cardinality = many; inverse = Training; transient = 0; }

	method NewTraining(Name:%Library.String,Place:%Library.String,BeginDate:%Library.Date,FinishDate:%Library.Date)
	{
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new a
			set a=##class(Person.Training).%New()
			set a.Name=Name
			set a.Place=Place
			set a.BeginDate=BeginDate
			set a.FinishDate=FinishDate
			do a.%Save()
			do a.%Close()
		]
	}

	method TrainingDISPLAYLIST()
	{
		returntype = %Library.String;
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new i,st
			set st=""
			set j=^Person.TrainingD
			for i=1:1:j {
				set Per=##class(Person.Training).%OpenId(i)
				set st=st_","_Per.Name
				do Per.%Close()
			}
			quit st
		]
	}

	storage Default
	{
		type = %Library.CacheStorage;
		datalocation = ^Person.TrainingD;
		indexlocation = ^Person.TrainingI;
		idlocation = ^Person.TrainingD;
		defaultdata = TrainingDefaultData;
		data TrainingDefaultData
		{
			structure = listnode;
			value (1)  = BeginDate;
			value (2)  = FinishDate;
			value (3)  = Name;
			value (4)  = Place;
		}
	}

}
