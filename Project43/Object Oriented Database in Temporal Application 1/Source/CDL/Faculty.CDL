/***********************************************************************\
|         File: Faculty.CDL                                             |
|       Author: Cache Objects Version 3.2.489.0                         |
|         Date: Apr 28 2001 12:54:57                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class Faculty
{

	description = "คลาส Faculty บอกลักษณะของวัตถุคณะ";
	super = %Persistent,TemporalMethod;

	persistent;

	attribute FcCode { type = %String(MAXLEN=2); calculated = 0; description = "รหัสคณะ  เป็น primary key ด้วย  ความยาว  char(2)"; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute FcDean { type = %String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute FcName { type = %String; calculated = 0; description = "ชื่อคณะภาษาไทย"; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute FcNameE { type = %String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }

	method AddFac(FacCode:%String,FacName:%String,FacNameE:%String,FacDean:%String)
	{
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		{
			new w 
			set w=##class(Faculty).%New()
			set w.FcCode=FacCode
			set w.FcName=FacName
			set w.FcNameE=FacNameE
			set w.FcDean=FacDean
			s ok=w.%Save()
			d w.%Close()
			
		}
	}

	method GiveOidFromFcName(FcName:%String)
	{
		returntype = %String;
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		{
			NEW rset,sc,result,i,ok
			SET rset=##class(%ResultSet).%New()
			SET rset.ClassName="Faculty"
			SET rset.QueryName="FindOidFromFcName"
			SET sc=rset.Execute(FcName)
			IF $$$ISERR(sc) ; The execute failed
			SET result=0
			FOR  QUIT:('rset.Next())!(result'=0)  DO
			. set i=rset.GetData(1)
			. set result=i
			DO rset.Close() ; Close this execution
			DO rset.%Close() ; Close the rset object
			QUIT result
		}
	}


	query ByFcCode((FcCode:%String))
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID,FcCode:%String,FcName:%String,FcNameE:%String,FcDean:%String"); 
		sqlquery =
		{
			:SELECT ID, FcCode, FcName, FcNameE, FcDean
			:FROM Faculty
			:WHERE (FcCode = :FcCode)
		}
		sqlproc = 0;
		sqlview = 0;
	}

	query ByName(())
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID,FcCode:%String,FcNameE:%String,FcName:%String,FcDean:%String"); 
		sqlquery =
		{
			:SELECT ID, FcCode, FcNameE, FcName, FcDean
			:FROM Faculty
		}
	}

	query Facultys(())
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID,FcCode:%String,FcName:%String,FcNameE:%String,FcDean:%String"); 
		sqlquery =
		{
			:SELECT ID, FcCode, FcName, FcNameE, FcDean
			:FROM Faculty
		}
		sqlproc = 0;
		sqlview = 0;
	}

	query FindOidFromFcName((FcName:%String))
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID"); 
		sqlquery =
		{
			:SELECT ID
			:FROM Faculty
			:WHERE (FcName = :FcName)
		}
		sqlproc = 0;
		sqlview = 0;
	}

	query GiveFacName(())
	{
		type = %SQLQuery(CONTAINID=0,ROWSPEC="FcName:%String"); 
		sqlquery =
		{
			:SELECT FcName
			:FROM Faculty
		}
		sqlproc = 0;
		sqlview = 0;
	}

	storage Default
	{
		type = %CacheStorage;
		datalocation = ^ooFacultyD;
		indexlocation = ^ooFacultyI;
		idlocation = ^ooFacultyD;
		defaultdata = FacultyDefaultData;
		data FacultyDefaultData
		{
			structure = listnode;
			value (1)  = FcAbbr;
			value (2)  = FcCode;
			value (3)  = FcCode1;
			value (4)  = FcName;
			value (5)  = FcNameE;
			value (6)  = FcDean;
		}
	}

}
