/***********************************************************************\
|         File: SubjectInCourse.CDL                                     |
|       Author: Cache Objects Version 3.2.489.0                         |
|         Date: Apr 28 2001 12:55:37                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class SubjectInCourse
{

	super = %Persistent,TemporalMethod;

	persistent;

	attribute CourseCode { type = %String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute SjCode { type = Subject; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute SjDateAdd { type = %Date; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute SjTerm { type = %Integer; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute SjType { type = %Integer(DISPLAYLIST=",วิชาบังคับ,วิชาบังคับเลือก,วิชาเลือกเสรี",VALUELIST=",1,2,3"); calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute SjYear { type = %Integer; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }

	method SjTypeDISPLAYLIST()
	{
		returntype = %String;
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		{
			#classcontext ..SjType
			quit ..#DISPLAYLIST
			#endclasscontext
		}
	}

	method aInit(CourseCode:%String,SjId:%Integer)
	{
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		{
			new a,b,ok
			set a=##class(SubjectInCourse).%New()
			set a.CourseCode=CourseCode
			set b=##class(SubjectInCourse).%OpenId(SjId)
			set a.SjCode=b
			set ok=a.%Save()
			do a.%Close()
		}
	}

	method aa(CourseCode:%String)
	{
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		{
			NEW col,rset,sc,columns,w,t
			SET rset=##class(%ResultSet).%New()
			SET rset.ClassName="SubjectInCourse"
			SET rset.QueryName="CourseId"
			SET columns=rset.GetColumnCount()
			SET sc=rset.Execute(CourseCode)
			IF $$$ISERR(sc) ; The execute failed
			FOR  QUIT:'rset.Next()  DO
			. FOR col=1:1:columns DO
			. . WRITE rset.GetColumnName(col),": "
			. . WRITE rset.GetData(col),!
			. . s w=##class(SubjectInCourse).%OpenId(rset.GetData(col))
			. . w "SjCode="_w.SjCode.SjCrd,!
			DO rset.Close() ; Close this execution
			DO rset.%Close() ; Close the rset object
			
		}
	}


	query ByCourse((CourseCode:%String))
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="SjCode->ID"); 
		sqlquery =
		{
			:SELECT SjCode->ID
			:FROM SubjectInCourse
			:WHERE (CourseCode = :CourseCode)
		}
		sqlproc = 0;
		sqlview = 0;
	}

	query ByCourseCode((CourseCode:%String))
	{
		type = %SQLQuery(CONTAINID=0,ROWSPEC="Val:%String"); 
		sqlquery =
		{
			:SELECT SjCode->SjName_Val
			:FROM SubjectInCourse
			:WHERE (CourseCode = :CourseCode)
		}
		sqlproc = 0;
		sqlview = 0;
	}

	query CourseId((CourseCode:%String))
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID"); 
		sqlquery =
		{
			:SELECT ID
			:FROM SubjectInCourse
			:WHERE (CourseCode = :CourseCode)
		}
		sqlproc = 0;
		sqlview = 0;
	}

	query CourseIdT((CourseCode:%String,Date:%Date))
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID"); 
		sqlquery =
		{
			:SELECT ID
			:FROM SubjectInCourse
			:WHERE (CourseCode = :CourseCode) AND (SjDateAdd = :Date)
		}
	}

	query GetForceSj((CourseCode:%String,SjType:%Integer))
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="SjCode->ID,ID,CourseCode:%String,SjYear:%Integer,SjTerm:%Integer"); 
		sqlquery =
		{
			:SELECT SjCode->ID, ID, CourseCode, SjYear, SjTerm
			:FROM SubjectInCourse
			:WHERE (CourseCode = :CourseCode) AND (SjType = :SjType)
			:ORDER BY SjYear DESC, SjTerm DESC
		}
	}

	query ReturnSubIdAsSjType1((CourseCode:%String,SjType:%Integer))
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID"); 
		sqlquery =
		{
			:SELECT ID
			:FROM SubjectInCourse
			:WHERE (CourseCode = :CourseCode) AND (SjType = :SjType)
		}
	}

	storage Default
	{
		type = %CacheStorage;
		datalocation = ^ooSubjectInCourseD;
		indexlocation = ^ooSubjectInCourseI;
		idlocation = ^ooSubjectInCourseD;
		defaultdata = SubjectInCourseDefaultData;
		data SubjectInCourseDefaultData
		{
			structure = listnode;
			value (1)  = CourseCode;
			value (2)  = SjCode;
			value (3)  = SjTerm;
			value (4)  = SjType;
			value (5)  = SjYear;
			value (6)  = Course;
			value (7)  = SjDateAdd;
		}
	}

}
