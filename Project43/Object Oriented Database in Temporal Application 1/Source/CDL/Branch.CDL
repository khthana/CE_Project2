/***********************************************************************\
|         File: Branch.CDL                                              |
|       Author: Cache Objects Version 3.2.489.0                         |
|         Date: Apr 28 2001 12:54:31                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class Branch
{

	abstract = 0;
	description = "คลาส  Branch อธิบายวัตถุ สาขาวิชา";
	super = %Persistent,TemporalMethod;

	persistent;

	attribute BrnCode { type = %String(MAXLEN=2); calculated = 0; description = "รหัสคณะความยาว 2 ตัว "; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute BrnDegree { type = %String(MAXLEN=40); calculated = 0; description = "ชื่อหลักสูตรภาษาไทย"; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute BrnDegreeE { type = %String(MAXLEN=40); calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute BrnDpt { type = Department; calculated = 0; description = "หรัสภาควิชา อ้างอิง มาจาก วัตถุ Department"; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute BrnNYear { type = %String; calculated = 0; description = "จำนวนปีของหลักสูตร"; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute BrnName { type = %String(MAXLEN=60); calculated = 0; description = "ขื่อสาขาวิชาภาษาไทย"; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute BrnNameE { type = %String(MAXLEN=60); calculated = 0; description = "ขื่อสาขาวิชาภาษาอังกฤษ"; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }

	method ReturnBrnCodeId(CodeFind:%String)
	{
		returntype = %String;
		classmethod;
		not final;
		public;
		sqlproc = 0;
		code =
		{
			NEW col,rset,sc,columnsend,sub,SubBrnCode,result,BrnNow,i
			SET rset=##class(%ResultSet).%New()
			SET rset.ClassName="Branch"
			SET rset.QueryName="SelTId"
			SET columns=rset.GetColumnCount()
			SET sc=rset.Execute(CodeFind)
			IF $$$ISERR(sc) ; The execute failed
			SET result=0
			FOR  QUIT:('rset.Next())!(result'=0)  DO
			. set i=rset.GetData(1)
			. set result=i
			DO rset.Close() ; Close this execution
			DO rset.%Close() ; Close the rset object
			QUIT result
		}
	}


	query BranInfo(())
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID,FcName:%String,DptName:%String,BrnName:%String"); 
		sqlquery =
		{
			:SELECT ID, BrnDpt->DptFc->FcName, BrnDpt->DptName, BrnName
			:FROM Branch
		}
	}

	query SelTId((Name:%String))
	{
		type = %SQLQuery(CONTAINID=1,ROWSPEC="ID"); 
		sqlquery =
		{
			:SELECT ID
			:FROM Branch
			:WHERE (BrnCode = :Name)
		}
		sqlproc = 0;
		sqlview = 0;
	}

	storage Default
	{
		type = %CacheStorage;
		datalocation = ^ooBranchD;
		indexlocation = ^ooBranchI;
		idlocation = ^ooBranchD;
		defaultdata = BranchDefaultData;
		data BranchDefaultData
		{
			structure = listnode;
			value (1)  = BrnCode;
			value (2)  = BrnDegree;
			value (3)  = BrnDpt;
			value (4)  = BrnFc;
			value (5)  = BrnName;
			value (6)  = BrnNameE;
			value (7)  = BrnStype;
			value (8)  = BrnType;
			value (9)  = BrnYesr;
			value (10)  = BrndegreeE;
			value (11)  = BrnDegreeE;
			value (12)  = BrnNYear;
		}
	}

}
