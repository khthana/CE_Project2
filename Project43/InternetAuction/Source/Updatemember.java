

//package auction;
import  checkdata ;
import  java.sql.*;
import  oracle.jdbc.driver.*;
import  java.io.*;
import  javax.servlet.*;
import  javax.servlet.http.*;
import  java.util.*;
public class Updatemember extends HttpServlet
{

    public void   doPost(HttpServletRequest req, HttpServletResponse resp)
    throws ServletException, java.io.IOException
  	{
	  resp.setContentType("text/html");
	  java.io.PrintWriter out = new java.io.PrintWriter(resp.getOutputStream());

    String username = req.getParameter("username");
	  String password = req.getParameter("password");
	  String confirm  = req.getParameter("confirm");
	  String email = req.getParameter("email");
	  String name = req.getParameter("name");
	  String surname = req.getParameter("surname");
	  String companyname = req.getParameter("companyname");
	  String sex = req.getParameter("sex");
	//  String BirthDate = req.getParameter("birthdate");
	//  String BirthMonth = req.getParameter("birthmonth");
	//  String BirthYear = req.getParameter("birthyear");
	  String age = req.getParameter("age");
	  String telephone = req.getParameter("telephone");
   // String date = BirthDate + "/" + BirthMonth + "/" + BirthYear;
    String date = req.getParameter("date");
 	  String region = req.getParameter("region");
    String type = req.getParameter("type");
	  String province = req.getParameter("province");
	  String telephonecompany = req.getParameter("telephonecompany");
	  String address = req.getParameter("address");
	  String zipcode = req.getParameter("zipcode");
	  String occupation = req.getParameter("occupation");
	  String pager = req.getParameter("pager");
	  String pagernumber = req.getParameter("pagernumber");
	  String mobilenumber = req.getParameter("mobilenumber");
	  String Str_Query;
    if (companyname.length() == 0)
        companyname = "-" ;
    out.println("<html>");
    out.println("<head>");
    out.println("<title> Employee List </title>");
	  out.println("</head>");
    out.println("company name = " + companyname);
    out.println("<h2><center>");
	  out.println("<br>");
    java.util.Properties props = new java.util.Properties();
    try
    {
     	  DriverManager.registerDriver (new oracle.jdbc.driver.OracleDriver());
	  }
	  catch (java.sql.SQLException ko){}
      Str_Query =  "update member set PASSWORD =" + "'" + password + "'" + "," ;
      Str_Query += "EMAIL     =" + "'" + email         + "'" + "," ;
      Str_Query += "COMP_NAME =" + "'" + companyname   + "'" + "," ;
      Str_Query += "NAME      =" + "'" + name          + "'" + "," ;
      Str_Query += "SURNAME   =" + "'" + surname       + "'" + "," ;
      Str_Query += "SEX       =" + "'" + sex           + "'" + "," ;
      Str_Query += "BIRTH_DATE=" + "to_date('" + date + "','DD/MM/YYYY')" + "," ;
      Str_Query += "TELEPHONE =" + "'" + telephone     + "'" + "," ;
      Str_Query += "AGE       =" + "'" + age           + "'" + "," ;
      Str_Query += "ADDRESS   =" + "'" + address       + "'" + "," ;
      Str_Query += "REGION    =" + "'" + region        + "'" + "," ;
      Str_Query += "PROVINCE  =" + "'" + province      + "'" + "," ;
      Str_Query += "ZIPCODE   =" + "'" + zipcode       + "'" + "," ;
      Str_Query += "TELE_COMP =" + "'" + telephonecompany  + "'" + "," ;
      Str_Query += "OCCUPATION=" + "'" + occupation        + "'" + "," ;
      Str_Query += "MOBILE    =" + "'" + mobilenumber      + "'" + "," ;
      Str_Query += "PAGER     =" + "'" + pager             + "'" + "," ;
      Str_Query += "PAGENUMBER=" + "'" + pagernumber       + "'" + "," ;
      Str_Query += "TYPE      =" + "'" + type              + "'" ;
      Str_Query += "where username='" + username +"'" ;
      out.println(Str_Query);
      query(Str_Query,props,out,email,resp);
	    out.println("success");
	    out.println("</html>");
      resp.sendRedirect("LoginSuccess");
	    out.flush();
    }
    boolean query (String sqlState,java.util.Properties props,java.io.PrintWriter out,
                         String email,HttpServletResponse resp)
   {
   boolean rc = true;
   Connection con = null;
   Statement stmt = null;
   ResultSet rs= null;
   int rowCount = 0;
   try
     {
      boolean have_user = false ;
      DriverManager.registerDriver (new oracle.jdbc.driver.OracleDriver());
      con = DriverManager.getConnection ("jdbc:oracle:thin:@161.246.5.182:1521:Internet","auction", "auction");
      stmt = con.createStatement();
      String finduser= "select email from member where email ='" + email + "'";
      out.println(finduser);
    	rs = stmt.executeQuery(finduser);
      while (rs.next())
		  {
        have_user = true ;
    	}

      if (have_user == true)
          resp.sendRedirect("../ExistUser.html");
      else
      rs = stmt.executeQuery(sqlState);
     }
   catch (Exception ex)
     {
	    out.println("Exception");
	    ex.printStackTrace(out);
	    rc = false;
	   }
   finally {
	   try
	   {
	    if (rs != null)
	    {
	     rs.close();
	    }
	   	if (stmt != null)
	   	{
		  stmt.close();
	   	}
   		if (con != null)
	  	{
        con.close();
  		}
 	   }
	   catch (Exception ex)
	   {
	   }
	   }
	   return rc;
		}

 }



















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































