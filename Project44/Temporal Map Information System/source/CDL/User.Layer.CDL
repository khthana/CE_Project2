/***********************************************************************\
|         File: User.Layer.CDL                                          |
|       Author: Cache Objects Version 4.0.818.0                         |
|         Date: Mar 19 2002 12:56:56                                    |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class User.Layer
{

	abstract = 0;
	not final;
	super = %Library.Persistent;

	persistent;

	array attribute Geometries { type = User.Geometry; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Map { type = User.Map; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Name { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }

	method SearchByName(Name:%Library.String)
	{
		returntype = %Library.List;
		classmethod = 0;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			new dquery,ok,colcount,rowcount,col	   
			set dquery=##class(%Library.ResultSet).%New("%Library.DynamicQuery:SQL") 	   	     	   	   
			set ok=dquery.Prepare("SELECT ID FROM Layer WHERE Name='"_Name_"'")
			set ok=dquery.Execute() 	   
			//Get Number of returned columns 
			set colcount=dquery.GetColumnCount() 	  
			//Get each row and write out the query column data 	  
			set idlist = $ListBuild()	    
			set rowcount = 0
			for  
			{ 
				quit:'dquery.Next() 
				//write ! 
				set rowcount=rowcount+1	   	
				for col=1:1:colcount 
				{ 
					set $LIst(idlist,rowcount) = dquery.GetData(col)	       		
				}	   	
			}
			//Close the resultset object and quit 
			set ok=dquery.%Close() 
			if rowcount>0
			{
				quit idlist
			}
		]
	}

	storage Default
	{
		type = %Library.CacheStorage;
		datalocation = ^User.LayerD;
		indexlocation = ^User.LayerI;
		idlocation = ^User.LayerD;
		defaultdata = LayerDefaultData;
		data Geometries
		{
			structure = subnode;
			value = Geometries;
			subscript = "Geometries";
		}
		data LayerDefaultData
		{
			structure = listnode;
			value (1)  = name;
			value (2)  = Name;
			value (3)  = Map;
		}
	}

}
