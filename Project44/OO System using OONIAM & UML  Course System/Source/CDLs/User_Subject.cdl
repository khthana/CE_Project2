/***********************************************************************\
|         File: User_Subject.cdl                                        |
|       Author: Cache Objects Version 4.1.856.0                         |
|         Date: Mar 9 2002 15:53:07                                     |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class User.Subject
{

	super = User.Dummy;

	persistent;

	parameter CLASSNAME { default = "Subject"; not final; }

	attribute Credit { type = %Library.Float; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Credit1 { type = %Library.Float; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Credit2 { type = %Library.Float; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute English { type = User.SubjectDescription; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute ExtSubjectID { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute IsOf { type = User.Department; calculated = 0; not final; multidimensional = 0; public; required; sqlcomputed = 0; transient = 0; }
	array attribute PossibleGrades { type = User.ResultType; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	array attribute Prerequisites { type = User.Subject; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Status { type = %Library.Boolean; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	array attribute SubCourses { type = User.SubCourse; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute SubjectLevel { type = %Library.TinyInt; calculated = 0; not final; initialexpression = 0; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Thai { type = User.SubjectDescription; calculated = 0; not final; multidimensional = 0; public; required; sqlcomputed = 0; transient = 0; }


	query QueryIsOf((deptid:User.Department))
	{
		type = %Library.SQLQuery(CONTAINID=1,ROWSPEC="ID,Code:%Library.String,Name:%Library.String,Credit:%Library.Float,SubjectLevel:%Library.TinyInt"); 
		sqlquery =
		[
			:SELECT ID, Thai_Code, Thai_Name, Credit, SubjectLevel
			:FROM Subject
			:WHERE (IsOf = :deptid)
			:ORDER BY SubjectLevel
		]
	}

	query QuerySelect((x:%Library.String))
	{
		type = %Library.SQLQuery(CONTAINID=1,ROWSPEC="ID,Code:%Library.String,Name:%Library.String,Credit:%Library.Float"); 
		sqlquery =
		[
			:SELECT ID, Thai_Code, Thai_Name, Credit
			:FROM Subject
			:WHERE (ID = :x)
		]
		sqlproc = 0;
		sqlview = 0;
	}

	storage Default
	{
		type = %Library.CacheStorage;
		defaultdata = SubjectDefaultData;
		data PossibleGrades
		{
			structure = subnode;
			value = PossibleGrades;
			subscript = "PossibleGrades";
		}
		data Prerequisites
		{
			structure = subnode;
			value = Prerequisites;
			subscript = "Prerequisites";
		}
		data SubCourse
		{
			structure = subnode;
			value = SubCourse;
			subscript = "SubCourse";
		}
		data SubCourses
		{
			structure = subnode;
			value = SubCourses;
			subscript = "SubCourses";
		}
		data SubjectDefaultData
		{
			structure = listnode;
			subscript = "Subject";
			value (1)  = Credit;
			value (2)  = Credit1;
			value (3)  = Credit2;
			value (4)  = English;
			value (5)  = Status;
			value (6)  = Thai;
			value (7)  = ResultGrade;
			value (8)  = SubjectOf;
			value (9)  = ExtSubjectID;
			value (10)  = SubjectLevel;
			value (11)  = Department;
			value (12)  = IsOf;
		}
	}

}
