/***********************************************************************\
|         File: User_Course.cdl                                         |
|       Author: Cache Objects Version 4.1.856.0                         |
|         Date: Mar 9 2002 15:53:07                                     |
|  Description: This File is Generated by Export Utility                |
\***********************************************************************/

class User.Course
{

	abstract = 0;
	not final;
	super = User.Dummy;

	persistent;

	parameter CLASSNAME { default = "User.Course"; not final; }
	parameter PARENTCLASSNAME { not final; }

	array attribute Categories { type = Category; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute CourseLevel { type = %Library.TinyInt; calculated = 0; not final; initialexpression = 0; multidimensional = 0; public; required; sqlcomputed = 0; transient = 0; }
	attribute English { type = CourseDescription; calculated = 0; not final; multidimensional = 0; public; required; sqlcomputed = 0; transient = 0; }
	attribute ExtCourseID { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute IsOf { type = Department; calculated = 0; not final; multidimensional = 0; public; required; sqlcomputed = 0; transient = 0; }
	attribute Objective { type = %Library.Stream; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Period { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	array attribute PostGraduate { type = %Library.String; calculated = 0; not final; multidimensional = 0; public; not required; sqlcomputed = 0; transient = 0; }
	attribute Require { type = %Library.Float; calculated = 0; not final; multidimensional = 0; public; required; sqlcomputed = 0; transient = 0; }
	attribute Thai { type = CourseDescription; calculated = 0; not final; multidimensional = 0; public; required; sqlcomputed = 0; transient = 0; }

	method Examine(studentid:%Library.String)
	{
		returntype = %Library.TinyInt;
		classmethod = 0;
		not final;
		public;
		sqlproc = 0;
		code =
		[
			quit 1
		]
	}

	method GetClassName()
	{
		returntype = %Library.String;
		classmethod = 0;
		final;
		public;
		sqlproc = 0;
		code =
		[
			quit ..#CLASSNAME
		]
	}

	method GetParentClass()
	{
		returntype = %Library.String;
		classmethod = 0;
		final;
		public;
		sqlproc = 0;
		code =
		[
			quit ..#PARENTCLASSNAME
		]
	}


	query QueryDetails(())
	{
		type = %Library.SQLQuery(CONTAINID=1,ROWSPEC="ID,Name:%Library.String,Name:%Library.String,CourseLevel:%Library.TinyInt"); 
		sqlquery =
		[
			:SELECT ID, Thai_Name, English_Name, CourseLevel
			:FROM Course
			:ORDER BY CourseLevel
		]
	}

	query QueryIsOf((departmentid:User.Department))
	{
		type = %Library.SQLQuery(CONTAINID=1,ROWSPEC="ID,Name:%Library.String,Name:%Library.String,CourseLevel:%Library.TinyInt"); 
		sqlquery =
		[
			:SELECT ID, Thai_Name, English_Name, CourseLevel
			:FROM Course
			:WHERE (IsOf = :departmentid)
			:ORDER BY CourseLevel
		]
	}

	storage Default
	{
		type = %Library.CacheStorage;
		defaultdata = CourseDefaultData;
		data Categories
		{
			structure = subnode;
			value = Categories;
			subscript = "Categories";
		}
		data CourseDefaultData
		{
			structure = listnode;
			subscript = "Course";
			value (1)  = CourseOf;
			value (2)  = English;
			value (3)  = ExtCourseID;
			value (4)  = Objective;
			value (5)  = Require;
			value (6)  = Thai;
			value (7)  = Period;
			value (8)  = CourseLevel;
			value (9)  = IsOf;
		}
		data PostGraduate
		{
			structure = subnode;
			value = PostGraduate;
			subscript = "PostGraduate";
		}
		data Students
		{
			structure = subnode;
			value = Students;
			subscript = "Students";
		}
	}

}
