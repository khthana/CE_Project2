import java.util.*;

public class  ShoppingCart{
	private Vector itemOrdered;

	/** build empty shopping cart **/

	public ShoppingCart() {
		itemOrdered = new Vector();
	}

	public Vector getItemOrdered() {
		return(itemOrdered);
	}

	public void add(ItemOrder itemOrder) {
		itemOrdered.add(itemOrder);
	}

	public void remove(int ii) {
		itemOrdered.remove(ii);
	}
/*
	public int getPr(String catNo) {
		int pr = 0;
		Iterator i = itemOrdered.iterator();
		while (i.hasNext()) {
			ItemOrder itemOrder = (ItemOrder) i.next();
			if (itemOrder.getCatNo().equals(catNo))
			{
				pr = itemOrder.getPrNo();
			}
		}
		return pr;
	}
*/
}//ShoppingCart
