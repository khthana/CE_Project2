package routersim;
import java.util.*;

public class LineCmd implements Enumeration{
  String s[];
  int i = 0;
  LineCmd(String s[]){
    this.s = s;
  }
  public boolean hasMoreTokens(){
    return i < s.length;
  }
  public Object nextTokens(){
    if (i < s.length)
      return s[i++];
    return null;
  }
}