package routersim;
import java.util.*;

public class GobalConfig implements Enumeration{
  String s[];
  int i = 0;
  GobalConfig(String s[]){
    this.s = s;
  }
  public boolean hasMoreElements(){
    return i < s.length;
  }
  public Object nextElements(){
    if (i < s.length)
      return s[i++];
    return null;
  }
}