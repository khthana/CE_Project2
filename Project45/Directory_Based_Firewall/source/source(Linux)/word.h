char* read_file(size_t* );// read text file and return stream of char
char* cut_rule(char* ,const char* );// for cut rules from stream of file and return only rule for that user
char add_remove(char *);
